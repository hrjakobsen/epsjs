(misc.ps) run
(error.ps) run
(symbol.ps) run
(arimo-regular.ps) run
(arimo-bold.ps) run
(arimo-oblique.ps) run
(arimo-bold-oblique.ps) run

% set systemdict to readonly so that the user program cannot modify it
systemdict readonly pop